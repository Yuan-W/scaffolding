language: python

os:
  - linux

python:
  - "2.6"
  - "2.7"
  - "3.6"

install:
  - pip install -r hints_provider/requirements.txt
  - pip install -r stats_updater/requirements.txt
  
script: 
  - pytest -v